# @admin-core/layouts

布局和业务组件库（Layouts & Business Components）。

## Build

```bash
pnpm --filter @admin-core/layouts build
```

## CDN

本包会产出 UMD 文件，可通过 `unpkg/jsdelivr` 引入（发布后生效）：

- `unpkg`: `dist/index.umd.js`
- `jsdelivr`: `dist/index.umd.js`

