# 国际化支持

Admin Core 设计系统支持多语言主题名称和描述。

## 支持的语言

- 🇨🇳 中文（简体）`zh-CN`
- 🇺🇸 英文（美国）`en-US`

## 快速开始

### 设置语言

```typescript
import { setLocale } from '@admin-core/design'

// 设置为英文
setLocale('en-US')

// 设置为中文
setLocale('zh-CN')
```

### 获取当前语言

```typescript
import { getLocale } from '@admin-core/design'

const currentLocale = getLocale()
console.log(currentLocale) // 'zh-CN' 或 'en-US'
```

### 获取主题名称（国际化）

```typescript
import { getThemeName, getThemeDescription } from '@admin-core/design'

// 获取主题名称
const name = getThemeName('deep-teal')
console.log(name) // 中文: '深邃青', 英文: 'Deep Teal'

// 获取主题描述
const description = getThemeDescription('deep-teal')
console.log(description) 
// 中文: '2026流行色 - 深邃的青色，沉稳大气'
// 英文: '2026 Trending Color - Deep teal, calm and atmospheric'
```

### 获取所有主题（自动国际化）

```typescript
import { getThemeMetadata } from '@admin-core/design'

// 获取所有主题（根据当前语言返回）
const themes = getThemeMetadata()

themes.forEach(theme => {
  console.log(`${theme.icon} ${theme.name}: ${theme.description}`)
})
```

## Vue 组件中使用

```vue
<template>
  <div>
    <!-- 语言切换器 -->
    <select @change="changeLanguage">
      <option value="zh-CN">中文</option>
      <option value="en-US">English</option>
    </select>

    <!-- 主题选择器（自动显示当前语言） -->
    <select v-model="variant">
      <option v-for="theme in themes" :key="theme.id" :value="theme.id">
        {{ theme.icon }} {{ theme.name }}
      </option>
    </select>

    <!-- 主题描述 -->
    <p>{{ currentTheme?.description }}</p>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { 
  useTheme, 
  setLocale, 
  getThemeMetadata,
  type Locale 
} from '@admin-core/design'

const { variant } = useTheme()

// 响应式主题列表
const themes = ref(getThemeMetadata())

// 当前主题
const currentTheme = computed(() => 
  themes.value.find(t => t.id === variant.value)
)

// 切换语言
const changeLanguage = (event: Event) => {
  const locale = (event.target as HTMLSelectElement).value as Locale
  setLocale(locale)
  
  // 重新获取主题列表以更新语言
  themes.value = getThemeMetadata()
}
</script>
```

## API 参考

### setLocale(locale: Locale)

设置当前语言。

- **参数**:
  - `locale`: `'zh-CN' | 'en-US'`
- **返回**: `void`

### getLocale()

获取当前语言。

- **返回**: `'zh-CN' | 'en-US'`

### getThemeName(themeId: string)

获取主题名称（国际化）。

- **参数**:
  - `themeId`: 主题 ID（如 `'deep-teal'`）
- **返回**: `string` - 当前语言的主题名称

### getThemeDescription(themeId: string)

获取主题描述（国际化）。

- **参数**:
  - `themeId`: 主题 ID（如 `'deep-teal'`）
- **返回**: `string` - 当前语言的主题描述

### getThemeMetadata()

获取所有主题元数据（自动国际化）。

- **返回**: `ThemeMetadata[]` - 包含当前语言的主题列表

### getTranslations()

获取当前语言的所有翻译。

- **返回**: `ThemeTranslations` - 当前语言的翻译对象

## 添加新语言

如果你想添加新的语言支持：

1. 在 `packages/design/src/theme/i18n/` 目录下创建新的语言文件（如 `ja-JP.ts`）
2. 参考 `zh-CN.ts` 的格式添加翻译
3. 在 `packages/design/src/theme/i18n/index.ts` 中注册新语言
4. 更新 `Locale` 类型

示例：

```typescript
// ja-JP.ts
import type { ThemeTranslations } from './zh-CN'

export const jaJP: ThemeTranslations = {
  themes: {
    default: {
      name: 'クラシックブルー',
      description: 'ほとんどのシナリオに適したクラシックな青のテーマ',
    },
    // ... 其他主题
  },
}
```

```typescript
// index.ts
export type Locale = 'zh-CN' | 'en-US' | 'ja-JP'

export const locales: Record<Locale, ThemeTranslations> = {
  'zh-CN': zhCN,
  'en-US': enUS,
  'ja-JP': jaJP,
}
```

## 注意事项

1. **默认语言**: 默认为中文（`zh-CN`）
2. **持久化**: 语言设置不会自动持久化，需要自行保存到 localStorage
3. **响应式**: 切换语言后需要重新调用 `getThemeMetadata()` 获取更新后的主题列表
4. **回退**: 如果设置了不支持的语言，会自动回退到默认语言并输出警告

## 完整示例

```typescript
import { 
  setLocale, 
  getLocale, 
  getThemeMetadata,
  getThemeName,
  getThemeDescription 
} from '@admin-core/design'

// 1. 设置语言
setLocale('en-US')

// 2. 获取所有主题（英文）
const themes = getThemeMetadata()
console.log(themes[0].name) // 'Classic Blue'

// 3. 切换回中文
setLocale('zh-CN')

// 4. 重新获取主题（中文）
const themesZh = getThemeMetadata()
console.log(themesZh[0].name) // '经典蓝'

// 5. 获取单个主题信息
console.log(getThemeName('deep-teal')) // '深邃青'
console.log(getThemeDescription('deep-teal')) // '2026流行色 - 深邃的青色，沉稳大气'
```
